<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE"
        tools:ignore="ScopedStorage" />

    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <!--Android 13 细化权限-->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" />

    <permission android:name="com.ct.ertclib.dc.GET_FEATURE"/>

    <!--DC服务权限-->
    <uses-permission android:name="com.newcall.permission.BIND_DC_SERVICE" />
    <!--EC服务权限-->
    <uses-permission android:name="com.newcall.permission.BIND_EC_SERVICE" />
    <!--屏幕共享服务权限-->
    <uses-permission android:name="com.newcall.permission.BIND_SCREEN_SHARE_SERVICE" />

    <application
        android:name=".DCApplication"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/icon_ct_dc_shortcut"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:requestLegacyExternalStorage="true"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.DayNight.NoActionBar"
        tools:replace="android:supportsRtl,android:allowBackup"
        tools:targetApi="34">


        <activity
            android:name=".ui.activity.MiniAppExpandedActivity"
            android:excludeFromRecents="true"
            android:exported="true"
            android:configChanges="orientation|screenSize|fontScale|keyboard|keyboardHidden|layoutDirection|locale|mcc|mnc|navigation|screenLayout|smallestScreenSize|uiMode"
            android:screenOrientation="behind"
            android:taskAffinity=""
            android:theme="@style/TransparentWindowStyle"
            android:permission="com.ct.ertclib.dc.GET_FEATURE">
            <intent-filter>
                <action android:name="com.ct.ertclib.dc.MINI_APP_EXPANDED" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="CHANNEL"
            android:value="${CHANNEL_VALUE}"/>

    </application>

    <queries package="${applicationId}">
        <intent>
            <action android:name="android.media.action.IMAGE_CAPTURE">

            </action>
        </intent>
        <intent>
            <action android:name="android.media.action.ACTION_VIDEO_CAPTURE">

            </action>
        </intent>
    </queries>

<!--    定义进入SDK主页面的权限，需要相同签名-->
    <permission
        android:name="com.ct.ertclib.dc.main_permission"
        android:protectionLevel="normal" />

</manifest>